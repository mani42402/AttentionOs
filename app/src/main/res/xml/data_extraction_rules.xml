<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing this app stores may leave the device through an OS transfer.

  The previous rules excluded only the database and sharedpref domains. Settings live in
  DataStore under files/datastore/, which is the `file` domain, and the key material added in
  the security phase lives under files/keys/ — both would have ridden a device-to-device
  transfer. Every domain is now excluded explicitly rather than relying on allowBackup alone,
  so a future manifest change cannot silently start exporting user data.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
